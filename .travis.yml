language: java
env:
  global:
    - SKIP_IMAGE_CHECK=1
before_install:
- sudo apt-key adv --keyserver pgp.nic.ad.jp --recv-keys C22BD678
- whoami
- ls -la /etc/apt/
- ls /etc/apt/sources.list
- cat /etc/apt/sources.list
- echo "deb http://members.change-vision.com/repos/apt/ astah main" | sudo tee -a /etc/apt/sources.list
- sudo apt-get update
- sudo apt-get install astah-community
script: mvn test -f ./astah-script-plugin/pom.xml
